# MongoDB Configuration
MONGODB_URI=mongodb://admin:password@localhost:27017/market?authSource=admin
MONGODB_DB=market

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# Binance API
BINANCE_API_URL=https://api.binance.com

# CoinMarketCap API (for dominance and market cap data)
# Get your API key from: https://coinmarketcap.com/api/
# Sign up for a free account and generate an API key
CMC_API_KEY=your_cmc_api_key_here

# Telegram Bot Configuration
# Create a bot by messaging @BotFather on Telegram
# Get your bot token from: https://core.telegram.org/bots/api
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_PRICE_CHAT_ID=@livingcoinpricechannel
TELEGRAM_SIGNAL_CHAT_ID=@ftlssignalzhan

# Coins to monitor (comma-separated)
COINS=BTCUSDT,ETHUSDT,SOLUSDT,BNBUSDT,NEARUSDT,LINKUSDT,ADAUSDT

# Docker Image Configuration (optional)
# IMAGE_REGISTRY=                    # Docker registry URL (leave empty for local)
# IMAGE_PREFIX=market                # Image name prefix (default: market)
# IMAGE_VERSION=latest                # Image version tag (default: latest)

# API Gateway Authentication
# Enable API key authentication and provide a comma-separated list of valid keys
API_KEY_ENABLED=false
API_KEYS=example-key-1,example-key-2
# Clients should send `X-API-Key: <key>` or `Authorization: Bearer <key>`

# Observability
# Enable metrics and tracing; configure Jaeger collector endpoint when tracing is enabled
METRICS_ENABLED=true
TRACING_ENABLED=true
JAEGER_ENDPOINT=http://localhost:4318

